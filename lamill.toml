# lamill.toml — per-site config consumed by lamill (portfolio/ops) and rankmill (content/audits).
# [content] = stable identity, authored by hand. [todo] = volatile work state.
# rankmill output lives in sites/<domain>/rankmill-output/, not here.

schema = "lamill-toml-v1"

[deploy]
platform = "cf-pages"
production_branch = "main"
custom_domains = [
    "drdebug.dev",
]

[index]
indexnow_key = "db135bd06672c458a8673d1609449a84"
indexnow_enabled = true

[stack]
framework = "astro"

# Tracked todos for drdebug.dev. status: "done" | "open".
[[todo]]
status = "done"
task = "Build /fix/ pSEO section: errors content collection + Zod schema, dynamic route, /fix/ + per-platform indexes, screenshots convention, sitemap integration"

[[todo]]
status = "done"
task = "Add verification gate (verified/confidence/verificationNote/sources/optional lastVerified) + draft banner + noindex + draft pills"

[[todo]]
status = "done"
task = "Draft 6 real sourced errors (n8n x3 high-confidence, Lindy x3 medium); document schema + route structure + how-to-add in AI_AGENTS.md"

[[todo]]
status = "done"
task = "Verify Lindy drafts (x3, medium confidence) against the live platform — confirm exact UI strings, then set verified=true + lastVerified"

[[todo]]
status = "open"
priority = "medium"
task = "Verify n8n drafts (x3) against live n8n — spot-check exact wording across versions, then set verified=true + lastVerified"

[[todo]]
status = "open"
priority = "medium"
task = "Relay: capture verbatim error strings from a live relay.app workspace (no public corpus found) and replace relay-placeholder.json"

[[todo]]
status = "done"
task = "GSC post-deploy: verify sc-domain:drdebug.dev and submit https://drdebug.dev/sitemap-index.xml"

[[todo]]
status = "open"
priority = "low"
task = "Decide whether to commit pnpm-lock.yaml (AI_AGENTS.md says it should be the one tracked lockfile; currently untracked)"

[[todo]]
status = "open"
priority = "low"
task = "Capture real Lindy UI screenshots for the 3 verified /fix/lindy/ pages (input-too-long, insufficient-credits, trouble-completing-task); drop into public/screenshots/ as lindy-<slug>-step-<n>.png and set each fixSteps[].screenshot. Backfill/polish — decoupled from launch, pages ship text-only."

[content]
# Declarative content identity for this site.
# Authored by the human; consumed by rankmill to generate drafts and run audits.
# Leave fields empty until you have a deliberate answer. Empty is better than wrong.
site_type = ""            # e.g. "legal-compliance", "tool", "directory", "blog", "b2c"
primary_keyword = ""      # the one phrase this site should rank for
secondary_keywords = []   # 3-8 supporting phrases
icp = ""                  # ideal customer profile — one sentence, specific role + context
urgency_trigger = ""      # what makes the reader act now (deadline, penalty, pain)
penalty = ""              # cost of inaction, in their terms (dollars, risk, time)
tone = ""                 # e.g. "direct, technical, no fluff" or "warm, plainspoken"
law = ""                  # statute or regulation if applicable, else empty
